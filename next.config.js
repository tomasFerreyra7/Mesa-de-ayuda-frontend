/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // Habilita Server Actions si querés usarlos más adelante
  },
};

module.exports = nextConfig;
